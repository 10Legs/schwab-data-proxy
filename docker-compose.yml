volumes:
  schwab-token:

services:
  init:
    build: .
    entrypoint: ["python", "/app/bootstrap.py"]
    command: []
    stdin_open: true
    tty: true
    env_file: .env
    volumes:
      - schwab-token:/data
    restart: "no"

  proxy:
    build: .
    depends_on:
      init:
        condition: service_completed_successfully
    ports:
      - "127.0.0.1:8080:8080"
    env_file: .env
    volumes:
      - schwab-token:/data
    restart: unless-stopped
    healthcheck:
      # Overrides Dockerfile HEALTHCHECK to extend start_period for multi-service stack startup.
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/healthz')"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3
